# Root env is reserved for Docker orchestration overrides.
# Per-app runtime env samples now live in:
# - web/.env.example
# - api/.env.example
# - worker/.env.example

# Docker Postgres published port on the host machine.
POSTGRES_PORT="55432"
